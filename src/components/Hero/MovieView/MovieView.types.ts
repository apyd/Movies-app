export interface IMovieViewProps {
    id: string,
    title: string,
    rating: string,
    poster_url: string,
    genres: string[],
    release_year: string,
    runtime: string,
    description: string,
}